
<% if book.favorited_by?(current_user) %>
  <%= link_to(book_favorites_path(book.id), method: :delete, remote: true, class: "text-danger") do %>
    <i class="fas fa-heart"></i><%= book.favorites.count %>
  <% end %>
<% else %>
  <%= link_to(book_favorites_path(book.id), method: :post, remote: true, class: "text-primary") do %>
    <i class="fas fa-heart"></i><%= book.favorites.count %>
  <% end %>
<% end %>
